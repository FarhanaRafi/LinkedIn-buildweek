// import { View, Text } from "react-native";
import React from "react";
import { Modal } from "react-bootstrap";

const PostImageModal = () => {
  return (
    <>
      <Modal.Header closeButton>PostImageModal</Modal.Header>
    </>
  );
};

export default PostImageModal;
